spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:mysql://192.168.1.8:3306/db_example
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root
    hikari:
      idle-timeout: 10000
      maximumPoolSize: 20
  jpa:
    hibernate:
      ddl-auto: update
hibernate:
  generate_statistics: true